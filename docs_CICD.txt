** Build CICD bằng gitlab-runner
* Cài gitlab-runner => server muốn deploy
B1. apt-get update
B2. curl -L "https://packages.gitlab.com/install/repositories/runner/gitlab-runner/script.deb.sh" | bash
B3. apt-get install gitlab-runner
B4. gitlab-runner -v :Kiểm tra version

* Các bước triển khai CICD
B1. Chọn nhánh cần kết nối, Lên gitlab project => Vào setting => Vào CICD => Chọn Runner => Expland
=> Ta thấy Register the runner with this URL: http://gitlab.huynqdevops.vn/ và token iLs9h8foYuoeHz7L8AxY
B2. Lên server chứa project
 - gitlab-runner register
 - Điền url register và token
 - description và tag điền giống tên server
 - executor có 3 chính: shell, docker, kubernetes => Newbie chọn shell
B3. Vào file: vi /etc/gitlab-runner/config.toml => concurrent = 1 // Có thể chạy đồng thời bao nhiêu dự án
B4. Khởi động gitlab-runner
nohup gitlab-runner run --working-directory /home/gitlab-runner/ --config /etc/gitlab-runner/config.toml --service gitlab-runner --user gitlab-runner 2>&1 &
=> Kiểm tra service chạy: ps -ef | grep gitlab-runner
B5. Vào CI/CD Settings của gilab kiểm tra 
 - Phần Available specific runners => chọn Edit => bỏ tích chọn When a runner is locked, it cannot be assigned to other projects

=> Hoàn thành cấu hình

* Viết kịch bản
B1. Tạo file .gitlab-ci.yml
stages:
    - build
    - deploy
    - checklog
build:
    stage: build
    script:
        - mvn install -DskipTests=true
    tags:
        - ec2-server
B2. Vào CICD -> pipeline để check build oke chưa
B3. Lên server kiểm tra: cd /home/gitlab-runner/builds/cp-eFsDZL/0/shoeshop/shoeshop
B4. Tạo folder: mkdir datas/shoeshop
* Lưu ý: trong dự án không chạy trực tiếp username gitlab-runner mà sẽ chạy username dự án
B5. Cấu hình cho gitlab-runner sử dụng sudo mà không cần password
    - visudo
    - Thêm 3 dòng sau root:
        + gitlab-runner ALL=(ALL) NOPASSWD: /bin/cp* => Phân quyền copy cho username gitlab-runner mà không cần password
        + gitlab-runner ALL=(ALL) NOPASSWD: /bin/chown* => Phân quyền sở hữu cho username gitlab-runner mà không cần password
        + gitlab-runner ALL=(ALL) NOPASSWD: /bin/su shoeshop* => Phân quyền chuyển user shoeshope cho username gitlab-runner mà không cần password
B6. Sửa lại file .gitlab-ci.yml trên gitlab
variables:
    projectname: shoe-ShoppingCart
    version: 0.0.1-SNAPSHOT
    projectuser: shoeshop
    projectpath: /root/datas/$projectuser/
stages:
    - build
    - deploy
    - showlog
build:
    stage: build
    variables:
        GIT_STRATEGY: clone
    script:
        - mvn install -DskipTests=true
    tags:
        - ec2-server
    only:
        - tags
deploy:
    stage: deploy
    variables:
        GIT_STRATEGY: none
    when: manual
    script:
        if("$GITLAB_USER_LOGIN"=='huynq1'); then
            sudo cp target/$projectname-$version.jar $projectpath
            sudo chown -R $projectuser. $projectpath
            sudo kill -9 $(ps -ef | grep $projectname-$version.jar | grep -v grep | awk '{print $2}')
            sudo su $projectuser -c "cd $projectpath; nohup java -jar target/$projectname-$version.jar > nohup.out 2>&1 &"
        else
            echo "Permision denined"
            exit 1
        fi
    tags:
        - ec2-server
    only:
        - tags
showlog:
    stage: showlog
    variables:
        GIT_STRATEGY: none
    when: manual
    script:
        - sleep 20
        - sudo su $projectuser -c "cd $projectpath; tail -n 1000 nohup.out"
    tags:
        - ec2-server
    only:
        - tags
      
Các variables gitlab ci: gitlab ci variables list
