========================================= Lộ trình Kubernetes ==========================================================
========================================= Mở đầu Kubernetes ============================================================
Cấu trúc Kubernetes: User / CLI  --->  Control Plane / Master Node  --->  Worker Nodes

1. Control Plane / Master Node: Chịu trách nhiệm quản lý toàn bộ cluster Kubernetes
Gồm các thành phần chính: API Server, etcd, Controller Manager (CM), Scheduler
- API Server thành phần trung tâm nhất của Kubernetes Control Plane có nhiệm vụ:
Nhận request từ kubectl, UI hoặc hệ thống bên ngoài
Kiểm tra request có hợp lệ không
Kiểm tra quyền truy cập
Ghi hoặc đọc trạng thái cluster từ etcd
Làm cổng giao tiếp giữa các thành phần Kubernetes

- etcd: Là nơi lưu trữ dữ liệu trạng thái Kubernetes gồm các thông tin: Danh sách Node, Danh sách Pod, Deployment, Service, ConfigMap, Secret, Namespace, Trạng thái mong muốn của ứng dụng, Trạng thái thực tế của cluster

- Controller Manager là thành phần chạy nhiều controller khác nhau trong Kubernetes.Controller có nhiệm vụ liên tục quan sát cluster và đảm bảo: Trạng thái thực tế = Trạng thái mong muốn. Ví dụ bạn khai báo muốn có 3 Pod, nhưng thực tế chỉ còn 2 Pod vì 1 Pod bị lỗi. Controller Manager sẽ phát hiện sự chênh lệch này và yêu cầu tạo thêm Pod mới.

- Scheduler chịu trách nhiệm chọn Worker Node phù hợp để chạy Pod mới. Khi một Pod mới được tạo ra, ban đầu Pod đó chưa biết sẽ chạy ở Node nào. Scheduler sẽ xem xét nhiều yếu tố để quyết định. Scheduler kiểm tra: Node nào còn CPU, Node nào còn RAM, Pod yêu cầu tài nguyên bao nhiêu, Node có label phù hợp không, Có taint/toleration không, Có affinity/anti-affinity không, Có giới hạn hoặc ràng buộc nào không. Sau đó Scheduler sẽ gán Pod vào một Node cụ thể.
2. Worker Node
a. Kubelet
- Kubelet là một agent chạy trên mỗi Worker Node. Nhiệm vụ chính của Kubelet là đảm bảo các container trong Pod đang chạy đúng theo yêu cầu từ Control Plane. Kubelet trên Worker Node sẽ nhận thông tin đó, rồi gọi Container Runtime để tạo container. Kubelet làm các việc như: Nhận yêu cầu chạy Pod từ API Server, Gọi Container Runtime để tạo container, Theo dõi trạng thái container, Restart container nếu bị lỗi, tùy theo restart policy, Báo trạng thái Pod và Node về API Server. Nó không tự quyết định chạy Pod nào. Việc Pod chạy ở Node nào là do Scheduler quyết định. Sau đó Kubelet thực thi việc chạy Pod đó.
b. Kube-Proxy
- Kube-Proxy là thành phần xử lý network trên mỗi Worker Node. Nó giúp các Pod và Service có thể giao tiếp với nhau.
Ví dụ: bạn có một Service tên là: payment-service, service này trỏ tới 3 Pod: payment-pod-1, payment-pod-2, payment-pod-3. Khi một request đi tới payment-service, Kube-Proxy sẽ giúp điều hướng request đó tới một trong các Pod phù hợp.
Kube-Proxy quản lý các rule mạng trên node, ví dụ: iptables, IPVS.
Nhiệm vụ của Kube-Proxy: Quản lý network rules, Cho phép Pod giao tiếp với Pod khác, Cho phép Service route traffic tới Pod, Hỗ trợ load balancing nội bộ giữa các Pod
c. Container Runtime
- Container Runtime là thành phần thực sự chạy container. Kubernetes không tự mình chạy container trực tiếp. Nó cần một runtime để làm việc này.
- Một số container runtime phổ biến: containerd, CRI-O, Docker
- Trong các phiên bản Kubernetes hiện đại, containerd thường được dùng phổ biến hơn Docker. Container Runtime làm các việc như: Pull image từ registry, Tạo container, Start container, Stop container, Quản lý vòng đời container
* Luồng đơn giản: 
API Server thông báo cho Kubelet
        ↓
Kubelet yêu cầu Container Runtime
        ↓
Container Runtime pull image và chạy container
        ↓
Container nằm trong Pod bắt đầu chạy


========================================= Cài đặt cụm Kubernetes Onpremise =============================================
# Link bài giảng: https://devopsedu.vn/courses/khoa-hoc-kubenetes-thuc-te/lesson/bai-5-trien-khai-kubernetes-cluster-tren-on-premise-2/?page_tab=overview
# Cài đặt cụm K8s trên On-premise
1. Cấu hình tài nguyên - thông tin server
ec2-server / 123456
Server              Hostname	        OS	                IP	    RAM (tối thiểu)	CPU (tối thiểu)     Disk            Domain
k8s-master-1     k8s-master-1	    Ubuntu 22.04	192.168.109.131	    3G	            2               20
k8s-master-2     k8s-master-2	    Ubuntu 22.04	192.168.109.132	    3G	            2               20
k8s-master-3     k8s-master-3	    Ubuntu 22.04	192.168.109.133	    3G	            2               20
rancher-server   rancher-server     Ubuntu 22.04	192.168.109.134	    2G	            1               20 && 10   rancher.elroydevops.vn
2. Thực hiện trên cả 3 servers
vi /etc/hosts
192.168.109.131 k8s-master-1
192.168.109.132 k8s-master-2
192.168.109.133 k8s-master-3
3. Tạo user devops và chuyển sang user devops trên 3 server: adduser devops
4. Phân quyền user devops vào group sudo để có quyền thực thi: chmod -aG sudo devops
5. Tắt swap để có thể call api của cụm: sudo swapoff -a => Chỉ tắt tạm thời khi server khởi động lại sẽ tự bật
Để tắt vĩnh viễn: vi /etc/fstab và comment /swap.img       none    swap    sw      0       0
hoặc dùng câu lệnh: sudo sed -i '/swap.img/s/^/#/' /etc/fstab
6. Cấu hình module kernel: sudo tee /etc/modules-load.d/containerd.conf <<EOF
overlay
br_netfilter
EOF
8. Chạy lệnh để Tải module kernel: sudo modprobe overlay và sudo modprobe br_netfilter
9. Cấu hình hệ thống mạng: sudo tee /etc/sysctl.d/kubernetes.conf <<EOF
net.bridge.bridge-nf-call-ip6tables=1
net.bridge.bridge-nf-call-iptables=1
net.ipv4.ip_forward=1
EOF
10. Chạy lệnh để Áp dụng cấu hình sysctl: sudo sysctl --system
11. Cài các gói phụ trợ để Cài đặt các gói cần thiết và thêm kho Docker:
    # sudo apt install -y curl gnupg2 software-properties-common apt-transport-https ca-certificates
    # sudo curl -fsSL https://download.docker.com/linux/ubuntu/gpg | sudo gpg --dearmour -o /etc/apt/trusted.gpg.d/docker.gpg
    # sudo add-apt-repository "deb [arch=amd64] https://download.docker.com/linux/ubuntu $(lsb_release -cs) stable" 
12.Cài đặt containerd
    # sudo apt update -y
    # sudo apt install -y containerd.io
13.Cấu hình containerd
    # containerd config default | sudo tee /etc/containerd/config.toml >/dev/null 2>&1
    # sudo sed -i 's/SystemdCgroup = false/SystemdCgroup = true/g' /etc/containerd/config.toml
14.Khởi động containerd
    # sudo systemctl restart containerd
    # sudo systemctl enable containerd
15.Thêm kho lưu trữ Kubernetes
    # sudo mkdir -p /etc/apt/keyrings
    # echo "deb [signed-by=/etc/apt/keyrings/kubernetes-apt-keyring.gpg] https://pkgs.k8s.io/core:/stable:/v1.30/deb/ /" | sudo tee /etc/apt/sources.list.d/kubernetes.list
    # curl -fsSL https://pkgs.k8s.io/core:/stable:/v1.30/deb/Release.key | sudo gpg --dearmor -o /etc/apt/keyrings/kubernetes-apt-keyring.gpg
16.Cài đặt các gói Kubernetes
    # sudo apt update -y
    # sudo apt install -y kubelet kubeadm kubectl
    # sudo apt-mark hold kubelet kubeadm 

*Mô hình đầu tiên:  1 master 2 worker

    -Thực hiện trên server k8s-master-1 làm master
        # sudo kubeadm init
        # mkdir -p $HOME/.kube
        # sudo cp -i /etc/kubernetes/admin.conf $HOME/.kube/config
        # sudo chown $(id -u):$(id -g) $HOME/.kube/config
        # kubectl get nodes
        # thêm network calico để hoạt động cụm
        # kubectl apply -f https://raw.githubusercontent.com/projectcalico/calico/v3.25.0/manifests/calico.yaml
    -Thực hiện trên server k8s-master-2 và k8s master-3 làm worker
        # sudo kubeadm join 192.168.1.111:6443 --token your_token --discovery-token-ca-cert-hash your_sha 
        # ex: sudo kubeadm join 192.168.109.131:6443 --token 8frm5a.ku9amr3mclw623p9 \ --discovery-token-ca-cert-hash sha256:8bb7f14eada01ee1af0adb3ad424e3e3528e3ac59b9e1fab58277ae73933fc6c

*Mô hình thứ hai: 3 server vừa master vừa worker – xem video bài giảng để hiểu

    -Thực hiện trên server k8s-master-1
        # sudo kubeadm init --control-plane-endpoint "192.168.109.131:6443" --upload-certs
        # mkdir -p $HOME/.kube 
        # sudo cp -i /etc/kubernetes/admin.conf $HOME/.kube/config 
        # sudo chown $(id -u):$(id -g) $HOME/.kube/config
        # kubectl apply -f https://raw.githubusercontent.com/projectcalico/calico/v3.25.0/manifests/calico.yaml
    -Thực hiện trên server k8s-master-2 và k8s-master-3
        # sudo kubeadm join 192.168.1.111:6443 --token your_token --discovery-token-ca-cert-hash your_sha --control-plane --certificate-key your_cert
        # vd: kubeadm join 192.168.109.131:6443 --token rxvl9p.1b9pq5w5dfvu3bx1
        --discovery-token-ca-cert-hash sha256:cd8ed7e337bee0ab2ddb899c0f76a06f8dbebbb58de06817ffadfd7bb804c10e
        --control-plane --certificate-key 526b41374a76375f8a8a47f9df153f2ef1ac0e3de29c3dd36185816608897a27
        # mkdir -p $HOME/.kube 
        # sudo cp -i /etc/kubernetes/admin.conf $HOME/.kube/config 
        # sudo chown $(id -u):$(id -g) $HOME/.kube/config
    -Chỉ định ô đang làm master làm cả worker:
        # kubectl taint nodes k8s-master-1 node-role.kubernetes.io/control-plane:NoSchedule-
Chú ý: Để reset trắng các cụm và cài lại dùng câu lệnh trên tất cả server:
    + sudo kubeadm reset -f
    + sudo rm -rf /var/lib/etcd
    + sudo rm -rf /etc/kubenetes/manifests/*

========================================= Cài đặt cụm Kubernetes Cloud bằng GCP (GKE) bình thường có 3 loại cloud: EKS, GKE, AKS ========================================
Link bài giảng: https://devopsedu.vn/courses/khoa-hoc-kubenetes-thuc-te/lesson/bai-6-trien-khai-kubernetes-cluster-tren-cloud-gke-2/
1. Lên google cloud platform => Tìm kiếm Kubernetes Cluster => Chọn cluster => Enable Kubernetes API Server
2. Vào Kubernestes Engine => Chọn cluster => Chọn Create
    - Chọn Standard: You manage your cluster

========================================= File Yaml cấu hình trong Kubernetes ==========================================
Trong Kubernetes, file cấu hình thường viết bằng YAML. File YAML dùng để khai báo tài nguyên như Namespace, Pod, Deployment, Service, Ingress, ConfigMap, Secret,...
Cấu trúc cơ bản của một file YAML Kubernetes:
apiVersion: apps/v1
kind: Deployment
metadata:
  name: shoeshop-app
  namespace: shoeshop
spec:
  replicas: 2
  selector:
    matchLabels:
      app: shoeshop
  template:
    metadata:
      labels:
        app: shoeshop
    spec:
      containers:
        - name: shoeshop
          image: nginx:latest
          ports:
            - containerPort: 80
Các phần chính:
    - apiVersion: Phiên bản API của Kubernetes resource.
        + Pod	v1
        + Service	v1
        + Namespace	v1
        + ConfigMap	v1
        + Secret	v1
        + ResourceQuota v1
        + Deployment	apps/v1
        + StatefulSet	apps/v1
        + DaemonSet	apps/v1
        + Job	batch/v1
        + CronJob	batch/v1
        + Ingress	networking.k8s.io/v1
    => Vd: Dùng cho Deployment => apiVersion: apps/v1
    - kind: là loại tài nguyên Kubernetes bạn muốn tạo.
        + Pod	Đơn vị nhỏ nhất, chứa container chạy app
        + Deployment	Deploy app, quản lý Pod, replica, rolling update
        + Service	Mở đường truy cập vào Pod
        + Namespace	Chia môi trường/project trong cluster
        + ConfigMap	Lưu cấu hình thường, ví dụ biến môi trường
        + Secret	Lưu thông tin nhạy cảm như password, token
        + Ingress	Routing HTTP/HTTPS từ bên ngoài vào service
        + PersistentVolume	Khai báo volume lưu trữ vật lý
        + PersistentVolumeClaim	App yêu cầu dung lượng lưu trữ
        + StatefulSet	Chạy app có trạng thái như database
        + DaemonSet	Chạy một Pod trên mỗi Node
        + Job	Chạy task một lần rồi kết thúc
        + CronJob	Chạy task theo lịch
        + ReplicaSet	Đảm bảo số lượng Pod, thường do Deployment quản lý
        + ResourceQuota Giới hạn tài nguyên
    => VD: Dùng cho Deployment => kind: Deployment
    - metadata là phần chứa thông tin nhận diện của resource. resource này tên gì
        + metadata thường có các field hay dùng:
            name	Tên resource
            namespace	Namespace chứa resource (Là 1 không gian để có thể triển fe,be)
            labels	Nhãn để phân loại, match selector
            annotations	Ghi chú/cấu hình phụ cho tool khác
        VD: metadata: 
                name: shoeshop-app
                namespace: shoeshop
                labels:
                    app: shoeshop
                annotations: 
                    description: "Shoeshop Java application"
    - spec là phần khai báo bạn muốn resource đó chạy/cấu hình như thế nào. resource này phải làm gì, chạy ra sao
    VD :    spec:
                type: NodePort #expose service ra ngoài node
                selector: #service trỏ tới Pod có label app=shoeshop
                    app: shoeshop
                ports:
                    - port: 80 #port của service
                    targetPort: 80 #port container bên trong Pod
                    nodePort: 30080 #port mở trên node

========================================= Namespace trong Kubernetes ===================================================
Link bài giảng: https://devopsedu.vn/courses/khoa-hoc-kubenetes-thuc-te/lesson/bai-9-namespace-trong-kubernetes-2/
namespace là 1 không gian riêng để có thể triển khai fe,be
Các câu lệnh:
    - kubectl get pod: Hiển thị các pod
    - kubectl get pod --namespace default
    - kubectl get ns(hoặc namespace): Hiển thị các namespace ở trên k8s
    - kubectl create ns project-1: Tạo namespace với tên là project-1 vd: kubectl create ns shoeshop
    - kubectl delete ns project-1: Xóa namespace với tên là project-1 vd: kubectl delete ns shoeshop
    - kubectl apply -f ns.yaml: Để chạy chỉ định file ns.yaml tạo namespace
    - kubectl apply -f ns.yaml: Xóa namespce ở ns.yaml (chứ k phải xóa file)
    => Tuy nhiên, bình thường sẽ tạo 1 file yaml chứ không chạy bằng câu lệnh
Tạo file yaml:
B1. Tạo project: mkdir -p projects/project-1
B2. Tạo file yaml: vi ns.yaml
apiVersion: v1
kind: Namespace
metadata:
    name: project-1
B3: kubectl apply -f ns.yaml
B4. Tạo file resourcequota.yaml : Mục đích để giới hạn tài nguyên => vi resourcequota.yaml
apiVersion: v1
kind: ResourceQuota #sử dụng tài nguyên ResourceQuota
metadata:
    name: mem-cpu-quota #đặt tên tùy ý
    namespace: project-1 #trỏ đến namespace 
spec:
    hard:
        requests.cpu: "2" #giới hạn cpu là 2
        requests.memory: 4Gi # giới hạn ram là 4gb
========================================= Phương pháp triển khai dự án trên Kubernetes =================================
Link bài giảng: https://devopsedu.vn/courses/khoa-hoc-kubenetes-thuc-te/lesson/bai-10-phuong-phap-trien-khai-du-an-tren-kubernetes-hieu-qua-2/
# Tư duy triển khai dự án k8s chuẩn format nhanh chóng
Nhu cầu => Sử dụng công cụ giao diện => Trích xuất file yml => Nghiên cứu cú pháp, foramt, giá trị, template, các tùy chọn khác,... => Triển khai tương tự chuẩn format
========================================= Các công cụ quản lý K8s ===============================================
# K8s Management Tool
Command app: Dùng k9s
Desktop app: Dùng lens k8s
Windows app: Dùng rancher  => Hay dùng nhất
========================================= Cài đặt triển khai rancher & tích hợp cụm K8s lên rancher =============
B1. Tạo server rancher
Lưu ý: Tiến hành tạo thêm hard disk(root) => Thực tế ko lưu dữ liệu ổ cứng ở hệ điều hành mà sẽ tạo ra 1 ổ cứng khác rồi gán vào server và sẽ lưu dữ liệu ở ổ cứng đó và đảm bảo server bị lỗi(docker rangcher) thì dữ liệu vẫn còn và chỉ cần lấy dữ liệu lưu vào server khác ứng dụng vẫn chạy được.
B2. Mount ổ cứng vào server với thư mục mới
    - lệnh lsblk: để kiểm tra thông tin disk Sẽ thấy thông tin sda là disk ban đầu là sda gắn vào folder /, sdb chưa gắn folder nào
    - Format ổ sdb: mkfs.ext4 -m 0 /dev/sdb
    - Tạo thư mục mount ổ sdb: mkdir /data
    - Cấu hình map sdb vào /data: echo "dev/sdb /root/data etx4 defaults 0 0" | tee -a /etc/fstab
    - Gõ df -h kiểm tra
B3. Cài docker: apt install docker.io -y && apt install docker-compose -y
B4. Cài đặt rancher vào thư mục vừa mount tạo folder rancher: mkdir -p /data/rancher
vi docker-compose.yml
version: '3'
services:
    rancher-server:
        image: rancher/rancher:v2.9.2 # tùy vào version của k8s
        container_name: rancher-server
        restart: unless-stopped
        ports:
            - "80:80"
            - "443:443"
        volumes:
            - /root/data/rancher/data:/var/lib/rancher
        privileged: true

B5. Lấy password: docker logs  container-id  2>&1 | grep "Bootstrap Password:" =>  => Đổi pass về: Quochuy221003@!
B6. Cài helm install: kubectl get secret --namespace cattle-system bootstrap-secret -o go-template='{{.data.bootstrapPassword|base64decode}}{{"\n"}}'
B7. Tích hợp k8s vào ranger => Chọn Home => Import Existing => Cluster => Generic => Điền tên sao cho tường minh => Sau đó copy dòng tự ký là dòng 2: curl --insecure -sfL https://192.168.109.134/v3/import/gqmbnl8mccbgjlvlkmxqgrjp4646rcq2v9qkbm6n5xbxbz2vkfvnpq_c-m-rkkc2bv9.yaml | kubectl apply -f -
=> Tích hợp thành công cụm k8s với 3 node (3 server)
========================================= Triển khai dự án K8s ==================================================
========================================= Triển khai công cụ trên K8s ===========================================
========================================= Giám sát và quản trị cụm K8s ==========================================
========================================= K8s thực tế doanh nghiệp ==============================================